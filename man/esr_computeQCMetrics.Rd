% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bundle.R
\name{esr_computeQCMetrics}
\alias{esr_computeQCMetrics}
\title{Compute QC Metrics for Endometrial Data}
\usage{
esr_computeQCMetrics(counts, mat_t = NULL, pheno = NULL, group_col = "group")
}
\arguments{
\item{counts}{A matrix/data.frame of raw counts (genes × samples).}

\item{mat_t}{Optional transformed matrix (samples × genes). If provided, used
to compute filtering statistics.}

\item{pheno}{Optional data.frame with sample metadata; used for per-group
statistics if provided.}

\item{group_col}{Character scalar; name of the phenotype column for grouping.
Defaults to "group".}
}
\value{
A data.frame with columns:
\describe{
\item{sample_id}{Character; sample identifiers}
\item{library_size}{Numeric; total read counts per sample}
\item{pct_zeros}{Numeric; percentage of zero counts per sample}
\item{n_genes}{Numeric; number of genes (non-zero) per sample}
}
If \code{mat_t} is provided, additional columns:
\describe{
\item{genes_after_filter}{Numeric; number of genes after filtering}
}
If \code{pheno} is provided, the data.frame includes the group column.
}
\description{
Computes quality control metrics including library sizes, percentage of zeros,
and filtering statistics for endometrial RNA-seq data.
}
\examples{
data(gse201926_sample)
mat_t <- esr_transform_log1p_cpm(gse201926_sample$counts)
qc_metrics <- esr_computeQCMetrics(
    counts = gse201926_sample$counts,
    mat_t = mat_t,
    pheno = gse201926_sample$pheno
)
head(qc_metrics)
}
\references{
R Core Team (2025). R: A language and environment for statistical
computing. R Foundation for Statistical Computing, Vienna, Austria.
\url{https://www.R-project.org/}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotEndometrialStabilityBars}
\alias{plotEndometrialStabilityBars}
\title{Plot Endometrial Stability Selection Frequencies}
\usage{
plotEndometrialStabilityBars(
  signature,
  annot = NULL,
  frequency_type = c("bootstrap", "selection", "both"),
  top_n = NULL,
  threshold = NULL,
  show_labels = TRUE,
  color_palette = NULL
)
}
\arguments{
\item{signature}{A list from \code{esr_loadPretrainedSignature()} or \code{esr_trainEndometrialSignature()} containing:
\describe{
\item{panel}{Character vector of gene IDs}
\item{stability}{List with \code{bootstrap_frequency} (named numeric vector) or \code{selection_frequency} (named integer vector)}
}
Alternatively, \code{stability} can be a named numeric vector directly.}

\item{annot}{Optional data.frame with gene annotations containing \code{gene_id} (or \code{GeneID}) and \code{Symbol} (or \code{symbol}) columns.}

\item{frequency_type}{Character scalar; which frequency to plot: "bootstrap" (default, from stability selection), "selection" (from outer CV), or "both" (if both available). Defaults to "bootstrap".}

\item{top_n}{Integer; number of top genes to show (by frequency). If NULL, shows all genes. Defaults to NULL.}

\item{threshold}{Numeric; minimum frequency threshold (0-1). Only genes with frequency >= threshold are shown. If NULL, no threshold filtering. Defaults to NULL.}

\item{show_labels}{Logical; whether to show gene labels on y-axis. Defaults to TRUE.}

\item{color_palette}{Optional color palette function or vector. Defaults to color-blind friendly gradient.}
}
\value{
A ggplot object showing stability selection frequency bar plot.
}
\description{
Creates a bar plot showing selection frequencies for each gene from stability selection.
Higher frequencies indicate more stable gene selection across bootstrap resampling or cross-validation.
}
\details{
This function visualizes stability selection frequencies from bootstrap resampling or cross-validation.
Higher frequencies (closer to 1.0) indicate genes that are consistently selected across resampling
iterations, providing more robust feature selection.

The plot can filter genes by frequency threshold and show top genes by frequency. Gene symbols can
be added via the \code{annot} parameter.
}
\examples{
\dontrun{
# Load pre-trained signature
signature <- esr_loadPretrainedSignature()

# Create stability bars plot (if stability info available)
if (!is.null(signature$stability)) {
  p_stab <- plotEndometrialStabilityBars(
    signature = signature,
    frequency_type = "bootstrap",
    top_n = 20,
    threshold = 0.6
  )
  print(p_stab)
}
}

}

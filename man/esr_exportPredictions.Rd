% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{esr_exportPredictions}
\alias{esr_exportPredictions}
\title{Export Predictions to CSV}
\usage{
esr_exportPredictions(
  predictions,
  dir = "export",
  formats = "csv",
  filename = NULL,
  ...
)
}
\arguments{
\item{predictions}{A data.frame from \code{esr_classifyEndometrial()} or a list with \code{predictions} element.}

\item{dir}{Character; output directory. Defaults to "export".}

\item{formats}{Character vector; which formats to export (currently only "csv").
Defaults to "csv".}

\item{filename}{Character; output filename. Defaults to "endometrial_predictions.csv".}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly, a character vector of paths to exported files.
}
\description{
Exports predictions from \code{esr_classifyEndometrial()} to CSV format.
}
\details{
This function exports predictions to CSV format with columns:
\itemize{
\item sample: Sample IDs
\item score: Raw signature scores
\item probability: Calibrated or sigmoid-transformed probabilities
\item prediction: Binary predictions (PS/PIS)
\item confidence_lower: Lower confidence bound (if available)
\item confidence_upper: Upper confidence bound (if available)
}
}
\examples{
\dontrun{
# Load signature and classify samples
signature <- esr_loadPretrainedSignature()
data(gse201926_sample)
predictions <- esr_classifyEndometrial(
  X_new = gse201926_sample$counts,
  signature = signature
)

# Export predictions
export_path <- esr_exportPredictions(
  predictions = predictions,
  dir = tempdir()
)

# Read exported file
read.csv(export_path)
}

}

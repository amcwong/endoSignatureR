% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-loading.R
\name{esr_loadCountsFromFile}
\alias{esr_loadCountsFromFile}
\title{Load Counts Data from File}
\usage{
esr_loadCountsFromFile(
  file_path,
  gene_id_col = "GeneID",
  sample_id_col = NULL,
  transpose = FALSE,
  ...
)
}
\arguments{
\item{file_path}{Path to counts file (TSV or CSV). May be compressed (.gz).}

\item{gene_id_col}{Character scalar; name of the gene ID column. Defaults to "GeneID".}

\item{sample_id_col}{Optional character scalar; name of sample ID column. If NULL, uses column names after gene_id_col.}

\item{transpose}{Logical; if TRUE, input is samples × genes and will be transposed. Defaults to FALSE.}

\item{...}{Additional arguments passed to \code{readr::read_tsv()} or \code{readr::read_csv()}.}
}
\value{
Matrix with genes as rows (rownames = GeneIDs) and samples as columns (colnames = sample IDs).
}
\description{
Loads raw counts data from TSV or CSV file and converts it to the matrix format
expected by package functions (genes × samples).
}
\details{
This function loads raw counts data from user files and converts it to the format expected
by package functions. The function:
\itemize{
\item Auto-detects TSV vs CSV format based on file extension
\item Handles compressed files (.gz) automatically
\item Converts data to numeric matrix format
\item Sets rownames to GeneIDs and colnames to sample IDs
\item Cleans sample IDs (removes quotes, trims whitespace)
}
}
\examples{
\dontrun{
# Load counts from TSV file
counts <- esr_loadCountsFromFile("my_counts.tsv")
dim(counts)
head(rownames(counts)) # Gene IDs
head(colnames(counts)) # Sample IDs

# Load from compressed CSV file
counts <- esr_loadCountsFromFile("my_counts.csv.gz", gene_id_col = "EnsemblID")
}

}
\references{
Wickham, H., Hester, J., & Bryan, J. (2024). readr: Read rectangular
text data. R package. \url{https://CRAN.R-project.org/package=readr}.
}

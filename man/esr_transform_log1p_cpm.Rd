% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{esr_transform_log1p_cpm}
\alias{esr_transform_log1p_cpm}
\title{Transform Counts to log1p-CPM}
\usage{
esr_transform_log1p_cpm(X, cpm_min = 1, cpm_min_samples = 4)
}
\arguments{
\item{X}{A matrix/data.frame of raw counts (genes x samples).}

\item{cpm_min}{Minimum CPM threshold for gene filtering. Defaults to 1.}

\item{cpm_min_samples}{Minimum number of samples that must meet CPM threshold. Defaults to 4.}
}
\value{
A transformed matrix (samples x genes) ready for PCA/visualization.
Rows are samples, columns are genes. Genes failing CPM filter are removed.
}
\description{
Converts raw counts to counts per million (CPM) with log1p transformation,
optionally filtering low-expressed genes.
}
\examples{
data(gse201926_sample)
mat_t <- esr_transform_log1p_cpm(gse201926_sample$counts)
dim(mat_t)  # samples x genes
}

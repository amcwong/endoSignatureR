% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-loading.R
\name{esr_loadPhenoFromFile}
\alias{esr_loadPhenoFromFile}
\title{Load Phenotype/Metadata from File}
\usage{
esr_loadPhenoFromFile(
  file_path,
  sample_id_col = NULL,
  group_col = NULL,
  format = c("auto", "series_matrix", "tsv", "csv"),
  ...
)
}
\arguments{
\item{file_path}{Path to phenotype file (series matrix, TSV, or CSV). May be compressed (.gz).}

\item{sample_id_col}{Optional character scalar; name of sample ID column. Auto-detected if NULL.}

\item{group_col}{Optional character scalar; name of group column. Auto-detected if NULL.}

\item{format}{Character scalar; file format: "auto" (default, auto-detect), "series_matrix", "tsv", or "csv".}

\item{...}{Additional arguments passed to \code{readr::read_tsv()} or \code{readr::read_csv()}.}
}
\value{
Data frame with required columns \code{sample_id} and \code{group}, plus any additional metadata columns.
}
\description{
Loads phenotype/metadata from series matrix file (GEO format) or simple TSV/CSV file
and converts it to the format expected by package functions.
}
\details{
This function loads phenotype/metadata from user files and converts it to the format expected
by package functions. The function:
\itemize{
\item Auto-detects series matrix format vs simple TSV/CSV
\item Handles compressed files (.gz) automatically
\item Auto-detects column names if not specified
\item Maps columns to expected format (\code{sample_id}, \code{group})
\item Validates required columns exist
}

For series matrix format, uses \code{endo_parse_metadata()} internally.
For simple TSV/CSV, auto-detects common column name patterns:
\itemize{
\item Sample ID: \code{sample_id}, \code{Sample_ID}, \code{sample}, \code{Sample}, \code{GSM_ID}, etc.
\item Group: \code{group}, \code{Group}, \code{label}, \code{Label}, \code{class}, \code{Class}, etc.
}
}
\examples{
\dontrun{
# Load from series matrix file (GEO format)
pheno <- esr_loadPhenoFromFile("my_series_matrix.txt", format = "series_matrix")

# Load from simple TSV file
pheno <- esr_loadPhenoFromFile("my_metadata.tsv")

# Load with explicit column names
pheno <- esr_loadPhenoFromFile("my_metadata.csv",
    sample_id_col = "SampleID",
    group_col = "Class"
)
}

}
